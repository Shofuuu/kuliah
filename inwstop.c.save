#include <stdio.h>

void detect(char* arg);
int main(int argc, char* argv[]){
	if(argc<2)
		printf("Kekurangan argumen!\nUsage : %s [INPUTAN]\n",argv[0]);
	else
		detect(argv[1]);
	return 0;
}
void detect(char* arg){
	short h=0, a=0;

	for(int check=0;check<sizeof(arg);check++){
		for(int x=0;x<9;x++)
			if(atoi(arg[check])==x)
	}
}
